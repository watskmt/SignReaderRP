version: "3.9"

services:
  postgres:
    image: postgres:15
    container_name: signreader_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: signreader_db
      POSTGRES_USER: signreader
      POSTGRES_PASSWORD: signreader_pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U signreader -d signreader_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7
    container_name: signreader_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
